
# Directory, program name (optional), package name (optional)
# If a "program" parameter is given, the script will inform you if that program isn't currently available as a command

[
  { dir: "niri", program: "niri" }
  { dir: "kitty", program: "kitty" }
  { dir: "fuzzel", program: "fuzzel" }
  { dir: "ironbar", program: "ironbar" }
  { dir: "nushell", program: "nu", package: "nushell" }
  { dir: "nvim", program: "nvim", package: "neovim" }
]

